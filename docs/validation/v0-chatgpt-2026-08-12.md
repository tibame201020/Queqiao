# Queqiao v0 ChatGPT validation

- Date: 2026-08-12
- Result: PASS
- Connector: `Queqiao_v0`
- Public MCP endpoint: `https://<funnel-host>/mcp`
- Environment: `windows`
- Workspace ID: `interview`
- Workspace root: `<windows-workspace-root>`
- OAuth scope: `workspace:read`

## Tool schema observed by ChatGPT

- `workspace_info`
- `read_file`

No other Queqiao tools were exposed in v0.

## Calls verified through ChatGPT

### `workspace_info`

Returned the expected environment, workspace ID, root, and read scope.

### `read_file`

Input:

```json
{
  "path": "mindset.txt",
  "offset": 0,
  "limit": 5
}
```

Returned lines 1-5 of 83 and the expected UTF-8 Chinese content.

## Proven path

```text
ChatGPT -> OAuth -> public Tailscale Funnel -> Queqiao Gateway
        -> authenticated localhost Worker -> Windows workspace -> mindset.txt
```

This document is the human acceptance evidence for the v0 compatibility baseline.
